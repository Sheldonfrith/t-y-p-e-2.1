import React from 'react';
import { Typography } from '@material-ui/core';

export default function CurrentStatsArea() {
    return (
        <div className="resultsArea">
            results area
            {/* <p>Current Speed: <b>{this.currentTypingSpeed}</b> wpm</p>
            <p>Accuracy: {this.getAccuracyPercentage()}% correct</p>
            <p>Current Wasted Keystrokes: {this.getWastedPercentage()}% </p>
            <div>Worst Character: <ol>{this.getCharSpeed('slow',1)}</ol></div> */}
          </div>
    );
}